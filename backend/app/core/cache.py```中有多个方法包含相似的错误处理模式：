def redis_error_handler(default_value=None):
    def decorator(func):
        async def wrapper(*args, **kwargs):
            try:
                return await func(*args, **kwargs)
            except RedisError as e:
                logger.error(f"{func.__name__}失败: {str(e)}")
                return default_value
        return wrapper
    return decorator 